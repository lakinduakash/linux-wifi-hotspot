# hotspotd environment overrides
# Copy this file to /etc/linux-wifi-hotspot/hotspotd.env and adjust values.

# Default SSID/password/interfaces when /etc/create_ap.conf is missing.
#HOTSPOTD_DEFAULT_SSID=DragonKit
#HOTSPOTD_DEFAULT_PASSPHRASE=ChangeMe1234
#HOTSPOTD_DEFAULT_WIFI_IFACE=wlan0
#HOTSPOTD_DEFAULT_INTERNET_IFACE=eth0

# Shared API token required for all HTTP requests. Make this a long, random string.
#HOTSPOTD_API_TOKEN=replace-me

# Comma-separated managed services exposed via the API (format id:systemd-unit[:description])
#HOTSPOTD_MANAGED_SERVICES=dragonsync:dragonsync.service:DragonSync OTA,vpn:vpnkit.service:VPN Tunnel
# Optional HTTP bind address (e.g., HTTP_LISTEN=127.0.0.1:8085)
#HTTP_LISTEN=127.0.0.1:8085
